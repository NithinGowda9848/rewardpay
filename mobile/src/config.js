// Config for React Native mobile application
// Replace localhost with your computer's local IP address so the mobile emulator/device can access the backend

export const BACKEND_URL = 'https://s-reward-pay.onrender.com';
export const API_URL = `${BACKEND_URL}/api`;
export const SOCKET_URL = BACKEND_URL;
