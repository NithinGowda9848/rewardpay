// Config for React Native mobile application
// Replace localhost with your computer's local IP address so the mobile emulator/device can access the backend

export const BACKEND_IP = '10.18.195.78'; // Replace with your development machine's local IP
export const API_URL = `http://${BACKEND_IP}:5000/api`;
export const SOCKET_URL = `http://${BACKEND_IP}:5000`;
